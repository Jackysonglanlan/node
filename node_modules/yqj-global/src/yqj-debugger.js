'use strict';

module.exports = {
  useGlobal: (opt) => {
    if(process.env.NODE_ENV === 'production') {
      global[opt] = () => () => {};
    }else {
      global[opt] = (namespace) => require('debug')(namespace);
    }
  }
}
